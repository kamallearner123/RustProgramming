///
/// This application takes input from user (std::io::stdin)
/// Calculated output gets displayes on Display (std::io::stdout)
/// Concpets:
/// - std::io
/// - Matching
/// - Error handling
/// - parse, expect, collect
/// 

use std::io::{Write, self};
fn main() {
    println!("Hello, world!");
}
